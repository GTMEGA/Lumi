static def curse(curseDep) {
    return "curse.maven:" + curseDep
}

def deobfCurse(curseDep) {
    try {
        return deobfMaven("https://cursemaven.com/", curse(curseDep))
    } catch (Exception ignored) {
        println("Failed to get dep " + curseDep + " from cursemaven. Grabbing from mirror.")
        return deobfMaven("https://mvn.falsepattern.com/cursemaven/", curse(curseDep))
    }
}

dependencies {
    // FalsePattern Lib
    implementation("com.falsepattern:falsepatternlib-mc1.7.10:[0.11,0.12):dev")

    // Chunk API
    implementation("com.falsepattern:chunkapi-mc1.7.10:[0.3.0,0.4.0):dev")

    // JetBrains Annotations
    compileOnly("org.jetbrains:annotations:[24,25)")

    // Lombok
    compileOnly("org.projectlombok:lombok:[1.18.24,1.19)")
    annotationProcessor("org.projectlombok:lombok:[1.18.24,1.19)")
}
